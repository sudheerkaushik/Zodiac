android {
            defaultConfig {
                applicationId = "org.nativescript.zodiac"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }